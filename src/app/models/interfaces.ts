export interface CreateUserModel {
    FirstName: string;
    LastName: string;
    Email: string;
    Password: string;
  }
  
export interface LoginUserModel {
    Email: string;
    Password: string;
}
